package org.synapseworks.pageharbor.ui.theme

import androidx.compose.material3.Typography

/** Material typography roles are used directly by screens; RME PDF Scanner does not need custom fonts. */
val PageHarborTypography = Typography()
